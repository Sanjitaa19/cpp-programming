#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int N, num;
    cin >> N;
    vector<int> numbers;

    for(int ctr = 1; ctr <= N; ctr++)
    {
        cin >> num;
        numbers.push_back(num);
    }

    for(auto val: numbers)
    {
        cout << val << " ";
    }

    return 0;
}
